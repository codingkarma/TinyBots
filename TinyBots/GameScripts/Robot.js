function Robot(id, name, speed, damage, hitpoints, energy) {
    this.id = id;
    this.name = name;
    this.speed = speed;
    this.damage = damage;
    this.hitpoints = hitpoints;
    this.energy = energy;
}
